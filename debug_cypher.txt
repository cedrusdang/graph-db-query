// List any 20 nodes and relationships
MATCH (n)-[r]->(m)
RETURN n, r, m
LIMIT 20;

// List 20 nodes
MATCH (n)
RETURN n
LIMIT 20;

// List 20 relationships
MATCH ()-[r]->()
RETURN r
LIMIT 20;

// Delete all
MATCH (n)
DETACH DELETE n;

// Debug:

// Count nodes by label
MATCH (n)
RETURN labels(n) AS label, count(*) AS count
ORDER BY count DESC;

// Count relationships by type
MATCH ()-[r]->()
RETURN type(r) AS rel_type, count(*) AS count
ORDER BY count DESC;

